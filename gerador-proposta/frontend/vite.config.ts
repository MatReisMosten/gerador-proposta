import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  build: {
    // FastAPI (api/main.py) serve o build a partir de ../frontend_dist
    // no mesmo processo/porta — sem CORS, um serviço só em produção.
    outDir: '../frontend_dist',
    emptyOutDir: true,
  },
  server: {
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:8000',
        changeOrigin: true,
      },
    },
  },
})
