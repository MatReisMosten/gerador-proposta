# Pacote de dados para deploy (Railway/Docker)

Gerado por `../prepare_deploy.sh`. Contém:

- `Modelo-Proposta-Tecnica-v1.0-variaveis.pptx` + `*-slots.json` (obrigatórios)
- `assets/Modelo-Proposta Tecnica v1.0.pptx` (fallback)
- `UNS001-26-vigia-valores.json` + `assets/logo-nph.png` (opcionais)

Não versionar os `.pptx` no git (estão no `.gitignore`).
